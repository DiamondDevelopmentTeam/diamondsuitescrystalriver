# Leave blank in local development because Vite proxies /api to the local server.
# For a separately hosted production API, use its full origin, for example:
# VITE_API_URL=https://api.diamondsuitescrystalriver.com
VITE_API_URL=
